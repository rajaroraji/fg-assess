version: 0.1
projects:
{{- range $project := matchPaths "../../vars/:env.tfvars" }}
    - path: .
      name: {{ $project.env }}
      terraform_var_files:
        - {{ $project._path }}
  {{- end}}
{{- end }}
